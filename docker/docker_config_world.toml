[database]
address = "db"
port = "3306"
user = "root"
password = "password"
database = "maplestory"

# Optional MySQL TLS settings:
# [database.tls]
# mode = "preferred"
# serverName = "db.example.com"
# caFile = "/etc/valhalla/mysql-tls/ca.pem"
# certFile = "/etc/valhalla/mysql-tls/client-cert.pem"
# keyFile = "/etc/valhalla/mysql-tls/client-key.pem"

[world]
message = "message"
ribbon = 2
expRate = 1.0
dropRate = 1.0
mesosRate = 1.0
autoBan = false
loginAddress = "login_server"
loginPort = "8485"
listenAddress = "0.0.0.0"
listenPort = "8584"
packetQueueSize = 512
